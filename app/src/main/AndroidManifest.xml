<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.introkotlin_901">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Introkotlin_901"
        tools:targetApi="31">
        <activity
            android:name=".ejemplo3.Cinepolis"
            android:exported="false" />
        <!-- Actividades del módulo ejemplo2 -->
        <activity
            android:name=".ejemplo2.ResultadoActivity"
            android:exported="false" />
        <activity
            android:name=".ejemplo2.SaludoActivity"
            android:exported="true" /> <!-- ✅ Cambiado a true -->
        <!-- Actividades del módulo ejemplo1 -->
        <activity
            android:name=".ejemplo1.SumaActivity"
            android:exported="true" /> <!-- Actividad principal -->
        <activity
            android:name=".MenuActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>